# Coding-agent extras (code indexing). Installed alongside requirements.txt.
# Upper bounds are mandatory here too — see the header of requirements.txt.
psycopg[binary]>=3.0,<4
psycopg_pool>=3.0,<4
cocoindex>=0.3.30,<1.0
numpy>=1.26,<3
